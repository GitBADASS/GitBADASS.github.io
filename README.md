# BADASS's docs site

The `deployment` branch contains the generated static site built from the source code stored in the `source` branch. It is intended solely for deployment to GitHub Pages.

➡️ Source branch: [`source`](../../tree/source)
